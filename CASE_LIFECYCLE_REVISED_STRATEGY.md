# 用例生命周期管理 - 重新设计方案

**问题**: 如果我们清除/归档历史用例，会丧失发现线上bug的能力  
**根本矛盾**: 需要保留所有历史用例来发现bug，但又需要控制执行范围

---

## 🤔 核心洞察

你的顾虑完全正确：

```
假设某个边界case在Week 1被发现：
  ✅ Week 1: 发现bug，创建测试用例
  ✅ Week 2-4: 继续运行，验证修复
  ❌ Week 5: 被自动归档/清除
  ❌ Week 8: 线上同样的bug再次出现
  💥 遗憾：用例已被删除，无法复现bug
```

**关键是**: 我们需要的是**分层执行**，不是**删除用例**！

---

## ✅ 推荐方案：三层执行模式

而不是删除用例，采用**变频执行**策略：

### 1️⃣ 活跃层 (Active Layer) - 周度执行 ⏰

```
用例来源: 最近 4 周的新生成用例
执行频率: 每周执行
执行范围: baseline + permanent + recent 4-week candidates/stable
目标时间: ~60 分钟

特点:
  - 新鲜的候选用例正在评估稳定性
  - 已验证稳定的用例继续监控
  - 核心回归用例必须通过
  - 快速反馈 (新bug立即发现)
```

### 2️⃣ 候选库 (Archive Layer) - 月度/季度执行 📅

```
用例来源: 历史 4+ 周前生成、但已验证有价值的用例
执行频率: 每月执行一次（或季度）
执行范围: 所有已晋升到 stable/permanent 的历史用例
目标时间: ~2-4 小时

特点:
  - 不在高频执行中，但定期检查
  - 发现回归bug（别的地方改动引发的bug）
  - 全量覆盖历史探索的功能边界
  - 可以发现那些"忘记的bug"再次出现
```

### 3️⃣ 存档库 (Deep Archive) - 按需查询 🔍

```
用例来源: 已完成使命或质量不达标的用例
执行频率: 不定期（按需）
执行范围: 当发现新bug时，可以快速查询+复活
目标时间: 按需执行

特点:
  - 完整保留所有历史用例
  - 不浪费日常执行资源
  - 如果发现新bug，可以：
    1. 查询存档 "这个功能的历史探索有什么?"
    2. 复活相关用例进行调查
    3. 可能发现原来的bug再次出现
```

---

## 📊 执行模式对比

| 方面 | 原方案 (无限累积) | 删除方案 ❌ | 分层方案 ✅ |
|------|----------|-----------|----------|
| 周度执行时间 | 🔴 无限增长 | 🟢 固定 60min | 🟢 固定 60min |
| 月度覆盖范围 | ❌ 无 | ❌ 无 | 🟢 全量历史 |
| 历史用例保留 | 🟢 全部保留 | 🔴 部分删除 | 🟢 全部保留 |
| 发现遗漏bug | 🟢 能 | 🔴 部分能 | 🟢 能 |
| 维护成本 | 🔴 高 | 🟢 低 | 🟡 中 |
| 执行成本 | 🔴 高 | 🟢 低 | 🟢 低 |

---

## 🎯 具体实现细节

### Week 1-2 (新用例探索阶段)

```
新生成用例进入 candidate 阶段
├─ 每周执行1次 (在活跃层中)
├─ 跟踪成功率
├─ 记录发现的任何bug
└─ 3周内100%成功 → 晋升到 stable
```

### Week 3-12 (稳定验证阶段)

```
稳定用例在活跃层中继续执行
├─ 每周执行1次
├─ 监控是否保持稳定
├─ 8周95%+成功 → 晋升到 permanent
└─ 如果失败率>10% → 降级回 candidate
```

### 12+ 周后 (移入候选库)

```
用例从活跃层移到候选库
├─ 不再在周度执行中出现
├─ 每月执行一次（集中月度)
├─ 检查是否仍然稳定
├─ 如果出现新失败 → 快速告警
└─ 如果某个相关功能改动 → 主动检查
```

### 异常状态 (按需复活)

```
如果发现类似的线上bug
├─ 查询存档库中的相关用例
├─ 找到历史探索
├─ 复活用例验证bug
├─ 可能发现"我们之前也遇到过"
└─ 快速修复已知问题
```

---

## 💾 存储结构

```
generated-cases/
├── manifest.json                 # 全局统计
├── weekly-diff/
│   ├── snapshot-2026-05-15/      # 活跃 (最近4周)
│   ├── snapshot-2026-05-08/      
│   ├── snapshot-2026-05-01/      
│   ├── snapshot-2026-04-24/      
│   │
│   ├── _archive/                 # 候选库 (历史4+周)
│   │   ├── snapshot-2026-04-17/
│   │   ├── snapshot-2026-04-10/
│   │   └── ...
│   │
│   └── _deep_archive/            # 深度存档 (完成使命/低质量)
│       ├── pr-32101/
│       ├── pr-32102/
│       └── ...

执行策略:
  周度: 执行 snapshot-2026-05-15/ 到 snapshot-2026-04-24/
  月度: 执行 _archive/ 中的所有
  按需: 查询 _deep_archive/
```

---

## 🔄 用例状态转移

```
┌─────────────┐
│  candidate  │  (新用例，周度执行)
│ 3周 100% ✅  │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│   stable    │  (已验证，周度执行)
│ 8周 95%+ ✅  │
└──────┬──────┘
       │
       ├─ 移入候选库 (12周后)
       │   └─ 每月执行1次
       │
       ├─ 晋升到永久 (持续稳定)
       │   └─ 每周必须通过
       │
       └─ 降级/失败
           └─ 进入 deprecated → _deep_archive

永久用例 (permanent)
  └─ 绝不删除，绝不停止执行
  └─ 核心回归用例
  └─ 每周必须通过，否则告警
```

---

## 📈 数据保留策略

### 什么永不删除

```
✅ 所有用例的元数据 (metadata) - 完全保留
✅ 所有运行历史 (runHistory) - 完全保留  
✅ 所有发现过的bug记录 - 完全保留
✅ 所有用例文件 - 完全保留在磁盘
```

### 只是改变执行频率

```
不是删除，而是:
  active (周度执行) → archive (月度执行) → deep_archive (按需)
  
数据完整性:
  ✅ 历史用例: 完全保留
  ✅ 历史数据: 完全保留
  ✅ 历史bug: 完全保留
  ✅ 快速复活: 随时可以恢复执行
```

---

## 🚨 特殊场景处理

### 场景1: 发现新的线上bug

```
问题: 线上发现bug，类似的功能流在历史用例中
解决:
  1. 查询存档库: "这个流程的用例有哪些?"
  2. 找到相关的存档用例
  3. 临时复活执行 (priority: high)
  4. 发现 "我们之前也遇到过这个bug"
  5. 快速对比: 修复方案是否回归了?
  6. 如果是回归: 告警 + 快速修复
```

### 场景2: 大功能改动

```
问题: 某个主要功能改动了
解决:
  1. 识别所有相关的历史用例
  2. 从候选库中激活相关用例
  3. 周度重新测试这些用例
  4. 确认修改没有破坏历史覆盖
  5. 验证后回归到月度执行
```

### 场景3: 发现某个用例总是失败

```
问题: 某个历史用例持续失败
解决:
  - 选项A: 修复基础代码
  - 选项B: 更新用例 (功能确实改了)
  - 选项C: 移入深度存档 (目标功能已下线)
  不是删除，而是有意识的重新分类
```

---

## 📊 监控指标

### 周度报告

```
活跃层执行:
  - 基础用例: 5 cases
  - 候选用例: 8 cases
  - 稳定用例: 12 cases
  - 永久用例: 10 cases
  - 总计: 35 cases, 执行时间 ~65 min
  - 通过率: 94% (2 个候选失败)
  - 新发现: 0 个bug
```

### 月度报告

```
候选库执行:
  - 执行用例: 120 cases (历史积累)
  - 执行时间: ~180 min
  - 通过率: 88%
  - 回归bug: 0 个
  - 建议晋升: 5 cases (连续 8 周 100%)
  - 建议降级: 2 cases (失败率 > 30%)
```

### 长期指标

```
积累价值:
  - 总用例数: 300+ (永不删除)
  - 活跃用例: 35 (周度)
  - 候选库: 200+ (月度抽样)
  - 深度存档: 60+ (按需)
  - 发现bug 总数: 18
  - 防止回归: 12 (已发现的bug再次出现时告警)
```

---

## ✅ 最终方案总结

**核心思想**: 不删除任何用例，只改变执行频率

```
┌──────────────────────────────────┐
│ 活跃层 (Active)                   │
│ ├─ 最近4周用例                    │
│ ├─ 周度执行 (~60min)              │
│ ├─ candidate + stable + permanent │
│ └─ 高频反馈，快速发现新bug        │
└──────────────────────────────────┘
        ↓ 4周后移出
┌──────────────────────────────────┐
│ 候选库 (Archive)                  │
│ ├─ 历史4+周用例                   │
│ ├─ 月度执行 (~180min)             │
│ ├─ 已验证稳定的用例              │
│ └─ 低频监控，发现回归bug          │
└──────────────────────────────────┘
        ↓ 特殊情况
┌──────────────────────────────────┐
│ 深度存档 (Deep Archive)           │
│ ├─ 完成使命/低质量用例            │
│ ├─ 按需执行                       │
│ ├─ 数据完全保留                   │
│ └─ 快速复活查询                   │
└──────────────────────────────────┘

所有层级: 数据完全保留 + 元数据完整 + 历史可追溯
```

---

## 🎯 实现优先级

1. **立即** (今天): 实现活跃层 + 基础分层存储
2. **本周**: 实现月度候选库执行计划
3. **下周**: 实现深度存档查询能力
4. **下月**: 完整的生命周期管理 UI

---

**关键结论**: 
✅ 保留所有数据 (永不丢失历史用例)  
✅ 控制执行范围 (周度固定60min)  
✅ 发现遗漏bug (月度全量检查)  
✅ 快速响应 (按需复活任何用例)
