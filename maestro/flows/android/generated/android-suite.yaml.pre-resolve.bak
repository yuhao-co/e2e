# Auto-generated Android Maestro Suite
# Generated: 2026-06-17T02:47:12.084Z
# 15 cases covering flight search results
# NOTE: Individual YAML files (<caseId>.yaml) are the primary execution method
#   This file is for reference and multi-document compatibility only.

appId: com.traveloka.android.staging
name: android-results-smoke
---
# Smoke: navigate home → search → results, verify flight cards appear
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000
- assertVisible:
    id: "card_result"

---

appId: com.traveloka.android.staging
name: android-results-filter-direct
---
# Test case for filtering direct (non-stop) flights
# Scenario: Open full filter dialog, apply Direct filter, verify results

# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Open full filter dialog
- tapOn:
    id: "flight_result_filter_button_title"
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"
    timeout: 15000

# Step 3: Apply Direct flights filter
- tapOn:
    id: "button_direct"
- tapOn:
    id: "dbwShow"

# Step 4: Wait for results to reload and verify
- extendedWaitUntil:
    visible:
      id: "text_result_title"
    timeout: 30000
- assertVisible:
    id: "card_result"

---

appId: com.traveloka.android.staging
name: android-results-sort-cheapest
---
# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Wait for card_result
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 3: Tap bm_button to open Sort tray
- tapOn:
    id: "flight_result_sort_button_title"          # floating sort suggestion pill at bottom of results page

# Step 4: Wait for rbg_sort to appear
- extendedWaitUntil:
    visible:
      id: "layout_tray"         # wait for Sort tray container to appear
    timeout: 10000

# Step 5: Tap radio_button index 0 for Cheapest option
- tapOn:
    id: "radio_button"
    index: 0                 # Cheapest option in Sort tray

# Step 6: Wait for card_result to reload
- extendedWaitUntil:        # [fix: Changed wait target to card_result instead of text_result_title]
    visible:
      id: "card_result"
    timeout: 30000

# Step 7: Assert card_result still visible
- assertVisible:
    id: "card_result"
---

appId: com.traveloka.android.staging
name: android-results-select-flight
---
# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Assert the results page is loaded
- assertVisible:         # [fix: Changed assertion to check for a visible element that is confirmed to exist on the results page]
    id: "card_result"

# Step 3: Tap first card_result
- tapOn:
    id: "card_result"
    index: 0  # Selecting the first flight card

# Step 4: Wait for fare selection screen
- extendedWaitUntil:
    visible:
      id: "flight_summary_activity_ticket_option_section"
    timeout: 30000

# Step 5: Assert fare selection screen is visible
- assertVisible:
    id: "flight_summary_activity_ticket_option_section"
---

appId: com.traveloka.android.staging
name: android-results-back-to-search
---
# Test case for navigating back to the search form from flight search results
# Scenario ID: android-results-back-to-search
# Description: Use system back gesture to return to search form
# Priority: p0

# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Wait for text_result_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "text_result_title"
    timeout: 30000  # [fix: ensure sufficient time for the toolbar title to appear]

# Step 3: Execute system back gesture
- back

# Step 4: Assert we leave the results page
- extendedWaitUntil:  # [fix: replaced assertVisible with extendedWaitUntil to ensure proper timeout handling]
    notVisible:
      id: "text_result_title"
    timeout: 30000  # [fix: ensure sufficient time to confirm the results page is exited]
---

appId: com.traveloka.android.staging
name: android-results-filter-one-stop
---
# Test case for applying 1-stop filter in flight search results
# Scenario ID: android-results-filter-one-stop
# Description: Open FULL filter dialog via flight_result_v4_filter_button, tap button_one_transit, apply with dbwShow

# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Open the full filter dialog
- tapOn:
    id: "flight_result_filter_button_title"  # Opens FlightResultRevampFilterDialog
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"           # Wait for filter dialog to appear
    timeout: 15000

# Step 3: Apply the 1-stop filter
- tapOn:
    id: "button_one_transit"               # Tap the 1-stop toggle
- tapOn:
    id: "dbwShow"                          # Apply/Show Results button

# Step 4: Wait for the results to update
- extendedWaitUntil:
    visible:
      id: "text_result_title"  # Wait for the results page to load
    timeout: 30000

# Step 5: Assert that the flight cards are still visible
- assertVisible:
    id: "card_result"  # Ensure that flight cards are visible after applying the filter
---

appId: com.traveloka.android.staging
name: android-results-sort-fastest
---
# Test case for sorting flights by the fastest duration
# Scenario ID: android-results-sort-fastest
# Description: Tap bm_button (floating sort pill), select radio_button index 1 (Shortest duration)

# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 3: Tap on the floating sort suggestion pill
- tapOn:
    id: "flight_result_sort_button_title"          # Floating sort suggestion pill

# Step 4: Wait for the sort tray to appear
- extendedWaitUntil:
    visible:
      id: "layout_tray"         # Wait for Sort tray container to appear
    timeout: 10000

# Step 5: Select the shortest duration sort option
- tapOn:
    id: "radio_button"      # Tap on the radio button for sorting
    index: 1                 # Index 1 corresponds to Shortest duration

# Step 6: Wait for the results page to load again
- extendedWaitUntil:        # [fix: added extendedWaitUntil to ensure results page reloads after sorting]
    visible:
      id: "card_result"
    timeout: 30000

# Step 7: Assert that the flight inventory cards are visible
- assertVisible:
    id: "card_result"  # Verify that flight cards are displayed after sorting
---

appId: com.traveloka.android.staging
name: android-results-airline-filter
---
# Scenario: Airline Filter via Bottom Sheet
# Description: Open FULL filter dialog via flight_result_v4_filter_button, scroll to reach layer_airline, tap check_box index 0, apply with dbwShow
# Priority: p1

# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Open the full filter dialog
- tapOn:
    id: "flight_result_filter_button_title"  # Opens FlightResultRevampFilterDialog
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"
    timeout: 15000

# Step 3: Scroll to reach airline section
- scrollUntilVisible:       # [fix: replaced bare scroll with scrollUntilVisible to ensure correct behavior]
    element:
      id: "layer_airline"
    direction: DOWN
    timeout: 15000

# Step 4: Tap the first airline checkbox
- tapOn:
    id: "check_box"             # First airline checkbox (flight_filter_airline_adapter_item.xml verified)

# Step 5: Apply the filter
- tapOn:
    id: "dbwShow"              # Apply button (NOT tvResult)
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 6: Assert that the inventory card is visible
- assertVisible:
    id: "card_result"  # Verify that the airline filter applied and cards updated
---

appId: com.traveloka.android.staging
name: android-results-departure-time-filter
---
# Test case for applying the morning departure filter in flight search results
# Scenario ID: android-results-departure-time-filter
# Description: Open FULL filter dialog, scroll to reach layer_time, tap button_departure_morning, apply with dbwShow

# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Open the full filter dialog
- tapOn:
    id: "flight_result_filter_button_title"  # Opens FlightResultRevampFilterDialog
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"
    timeout: 15000

# Step 3: Scroll to the departure time section
- scrollUntilVisible:
    element:
      id: "button_departure_morning"
    direction: DOWN
    timeout: 15000  # Ensure we can reach the button

# Step 4: Tap the morning departure button
- tapOn:
    id: "button_departure_morning"

# Step 5: Apply the filter
- tapOn:
    id: "dbwShow"  # Apply button in the filter dialog

# Step 6: Wait for the results to update
- extendedWaitUntil:     # [fix: added extended wait to ensure the results page is fully updated after applying the filter]
    visible:
      id: "card_result"
    timeout: 30000

# Step 7: Assert that the inventory card is visible, confirming the filter was applied
- assertVisible:
    id: "card_result"
---

appId: com.traveloka.android.staging
name: android-results-reset-filters
---
# Test case for resetting all filters in the flight search results
# Scenario ID: android-results-reset-filters
# Description: Open FULL filter dialog, apply direct filter, tap tvReset to clear, apply with dbwShow

# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Open the full filter dialog
- tapOn:
    id: "flight_result_filter_button_title"  # Opens FlightResultRevampFilterDialog
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"
    timeout: 15000

# Step 3: Apply direct filter
- tapOn:
    id: "button_direct"  # Select direct filter first so tvReset has something to clear

# Step 4: Reset all filters
- tapOn:
    id: "tvReset"  # Reset All button (flight_result_revamp_filter_dialog.xml verified)

# Step 5: Apply with no filters active
- tapOn:
    id: "dbwShow"  # Apply button with no filters active (NOT tvResult)

# Step 6: Wait for results to load
- extendedWaitUntil:      # [fix: added extendedWaitUntil to ensure results are fully loaded]
    visible:
      id: "card_result"
    timeout: 30000

# Step 7: Assert that inventory cards are visible
- assertVisible:
    id: "card_result"  # All filters cleared, full result set restored
---

appId: com.traveloka.android.staging
name: android-results-scroll
---
# Step 1: Launch the app and navigate to the search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Scroll through the results
- scroll              # First scroll to load more cards
- scroll              # Second scroll to load more cards

# Step 3: Assert that the flight result cards are still visible
- assertVisible:
    id: "card_result"  # Ensure that the flight cards are still visible after scrolling
---

appId: com.traveloka.android.staging
name: android-results-price-calendar
---
# Test case for viewing the price calendar from flight search results
# Scenario ID: android-results-price-calendar
# Description: Tap calendar icon at far-right of flight_result_v4_date_flow_row to open FlightBloomCalendarDialog, assert calendar_navbar_close visible

# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Wait for the date flow widget to be visible
- extendedWaitUntil:
    visible:
      id: "widget_dateflow"
    timeout: 10000

# Step 3: Tap the calendar icon at the right end of the date flow row
- tapOn:
    id: "image_calendar"   # Calendar icon inside widget_dateflow

# Step 4: Wait for the calendar dialog to be visible
- extendedWaitUntil:
    visible:
      id: "calendar_navbar_close"
    timeout: 10000

# Step 5: Assert that the calendar dialog is visible
- assertVisible:
    id: "calendar_navbar_close"
---

appId: com.traveloka.android.staging
name: android-results-filter-multi-stop
---
# Step 1: Launch the app and navigate to the search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Open the filter dialog
- tapOn:
    id: "flight_result_filter_button_title"  # Open full filter dialog
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"           # Wait for filter dialog to appear
    timeout: 15000

# Step 3: Apply the 2+ Stops filter
- tapOn:
    id: "button_two_transit"               # Select 2+ Stops filter option
- tapOn:
    id: "dbwShow"                          # Apply the filter (correct apply button)

# Step 4: Wait for the results to reload
- extendedWaitUntil:
    visible:
      id: "text_result_title"  # Wait for results to reload after applying filter
    timeout: 30000

# Success criteria: Verify that the results or empty-state is visible
- assertVisible:
    id: "card_result"  # Check that results are displayed after applying the filter
---

appId: com.traveloka.android.staging
name: android-results-sort-best
---
# Test case for sorting flights by Best option
# Scenario ID: android-results-sort-best
# Description: Tap sort index 2 (Best) in sort tray
# Priority: p2

# Precondition: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 1: Wait for the results page header to ensure the page is loaded
- extendedWaitUntil:
    visible:
      id: "text_result_title"
    timeout: 30000

# Step 2: Open the sort tray
- tapOn:
    id: "flight_result_sort_button_title"          # Floating sort suggestion pill at bottom of results page
- extendedWaitUntil:
    visible:
      id: "layout_tray"         # Wait for Sort tray container to appear
    timeout: 10000

# Step 3: Tap the radio button at index 2 (Direct flights first)
- tapOn:
    id: "radio_button"
    index: 2                 # Sort by Direct flights first

# Step 4: Wait for the results page header to reload
- extendedWaitUntil:
    visible:
      id: "text_result_title"
    timeout: 30000
---

appId: com.traveloka.android.staging
name: android-results-view-detail
---
# Step 1: Navigate to search results
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 2: Wait for card_result (Compose id)
- extendedWaitUntil:
    visible:
      id: "card_result"
    timeout: 30000

# Step 3: Tap card_result (Compose id)
- tapOn:
    id: "card_result"
    index: 0  # Assuming we want to tap the first flight card

# Step 4: Wait for detail overlay or new screen
- extendedWaitUntil:
    visible:
      id: "text_result_title"  # Assuming this ID is present in the detail view
    timeout: 30000

# Step 5: Take screenshot
- screenshot:
    path: "test-results/android/android-results-view-detail.png"

# Step 6: Tap back to return to results (system back)
- back

# Step 7: Assert text_result_title visible again (Compose id)
- assertVisible:
    id: "text_result_title"
---

# === P0: SSR V4 Multi-Filter: Transit Tab ===
appId: com.traveloka.android.staging
name: android-ssrv4-filter-tab-transit
---

# Scenario: SSR V4 Multi-Filter: Transit Tab
# Description: Open multi-filter dialog, navigate to Transit tab, apply Direct, verify results update
# Priority: p0

# Step 1: Stop the app and navigate to the flight search results page using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the filter button to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 3: Tap the filter button to open the multi-filter dialog
# [sanitized: bare tapOn removed — no target specified]

# Step 4: Wait for the filter dialog to appear
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 5: Tap the Transit tab inside the filter dialog
# [sanitized: bare tapOn removed — no target specified]

# Step 6: Tap the Direct/Nonstop option inside the Transit tab content
# [sanitized: bare tapOn removed — no target specified]

# Step 7: Tap the Apply/Done button to submit the filter
# [sanitized: bare tapOn removed — no target specified]

# Step 8: Wait for the results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 9: Assert that the results are visible
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P0: SSR V4 Multi-Filter: Departure Time Tab ===
appId: com.traveloka.android.staging
name: android-ssrv4-filter-tab-time
---

# Test Case: SSR V4 Multi-Filter: Departure Time Tab
# Description: Navigate to Time tab in multi-filter dialog, select morning departure
# Priority: p0

# Step 1: Stop the app and navigate to the Flight Search Results screen using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the Filter button to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 3: Tap on the Filter button to open the filter dialog
# [sanitized: bare tapOn removed — no target specified]

# Step 4: Wait for the filter dialog to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 5: Scroll to the Time tab in the filter dialog
- scrollUntilVisible:
    element:
    direction: DOWN
    timeout: 15000

# Step 6: Tap on the Morning departure option
# [sanitized: bare tapOn removed — no target specified]

# Step 7: Tap on the Apply button to apply the filter
# [sanitized: bare tapOn removed — no target specified]

# Step 8: Wait for the flight results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 9: Assert that the flight results are visible
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P1: SSR V4 Multi-Filter: Airlines Tab ===
appId: com.traveloka.android.staging
name: android-ssrv4-filter-tab-airlines
---

# Step 1: Stop the app and navigate to the flight search results page using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the filter button to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 3: Tap on the filter button to open the filter dialog
# [sanitized: bare tapOn removed — no target specified]

# Step 4: Wait for the filter dialog to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 5: Scroll to the Airlines tab in the filter dialog
- scroll # Scroll to make the Airlines tab visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 6: Tap on the first airline checkbox in the Airlines tab
- tapOn:
    index: 0 # First airline checkbox

# Step 7: Tap on the Apply button to apply the filter
# [sanitized: bare tapOn removed — no target specified]

# Step 8: Wait for the results to reload and ensure flight cards are visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Success Criteria: Verify that the airline filter is applied and results are visible
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P1: SSR V4 Multi-Filter: Price Tab ===
appId: com.traveloka.android.staging
name: android-ssrv4-filter-tab-price
---

# Step 1: Stop the app and navigate to the flight search results page using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the filter button to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 3: Tap on the filter button to open the filter dialog
# [sanitized: bare tapOn removed — no target specified]

# Step 4: Wait for the filter dialog to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 5: Scroll to the Price tab in the filter dialog
- scrollUntilVisible:
    element:
    direction: DOWN
    timeout: 15000

# Step 6: Interact with the price range slider to adjust the minimum price
- tapOn:
    point: [200, 1200] # NO ID IN SOURCE — coordinate fallback, Pixel7 1080×2400
    # Coordinates correspond to the approximate location of the price range slider handle
- wait: 1000 # Wait for the slider adjustment animation to complete

# Step 7: Tap on the Apply button to apply the filter
# [sanitized: bare tapOn removed — no target specified]

# Step 8: Wait for the flight results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 9: Verify that the price filter was applied successfully
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P1: SSR V4 Multi-Filter: Duration Tab ===
appId: com.traveloka.android.staging
name: android-ssrv4-filter-tab-duration
---

# Step 1: Stop the app and navigate to the Flight Search Results screen using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the filter button to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 3: Tap on the filter button to open the filter dialog
# [sanitized: bare tapOn removed — no target specified]

# Step 4: Wait for the filter dialog to become visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 5: Scroll to the Duration tab in the filter dialog
- scrollUntilVisible:
    element:
    direction: DOWN
    timeout: 15000

# Step 6: Adjust the duration slider to a restrictive value
- tapOn:
    point: [0.8, 0.5] # Adjust slider to a restrictive value (80% of the slider width)
    # NO ID IN SOURCE — coordinate fallback, Pixel7 1080×2400

# Step 7: Tap on the Apply button to apply the filter
# [sanitized: bare tapOn removed — no target specified]

# Step 8: Wait for the results to reload or an empty state to appear
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Success Criteria: Verify that the results or empty state are displayed
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P1: SSR V4 Multi-Filter: Airports Tab ===
appId: com.traveloka.android.staging
name: android-ssrv4-filter-tab-airports
---

# Step 1: Stop the app and navigate to the flight search results page using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the filter button to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 3: Tap on the filter button to open the filter dialog
# [sanitized: bare tapOn removed — no target specified]

# Step 4: Wait for the filter dialog to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 5: Scroll to and tap the Airports tab in the FilterTopTabBar
- scrollUntilVisible:
    element:
    direction: DOWN
    timeout: 15000
# [sanitized: bare tapOn removed — no target specified]

# Step 6: Tap the first departure airport option in the airports list
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000
- tapOn:
    index: 0

# Step 7: Tap the Apply button to apply the filter
# [sanitized: bare tapOn removed — no target specified]

# Step 8: Wait for the results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Success Criteria: Assert that the results page is visible and the filter is applied
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P2: SSR V4 Multi-Filter: Facilities Tab ===
appId: com.traveloka.android.staging
name: android-ssrv4-filter-tab-facilities
---

# Step 1: Stop the app and navigate to the Flight Search Results page using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the filter button to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 3: Tap on the filter button to open the filter dialog
# [sanitized: bare tapOn removed — no target specified]

# Step 4: Wait for the filter dialog to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 15000

# Step 5: Scroll the tab bar to find the Facilities tab and tap it
- scrollUntilVisible:
    element:
    direction: DOWN
    timeout: 15000
# [sanitized: bare tapOn removed — no target specified]

# Step 6: Tap the first facility option (e.g., meal/luggage) in the facilities list
- tapOn:
    index: 0

# Step 7: Tap the Apply button to apply the filter
# [sanitized: bare tapOn removed — no target specified]

# Step 8: Wait for the results to reload or an empty state to appear
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Success Criteria: Assert that the results are visible or an empty state is displayed
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P0: SSR V4 Mini-Filter Tray Opens ===
appId: com.traveloka.android.staging
name: android-ssrv4-mini-filter-tray-open
---

# Step 1: Stop the app and navigate to the flight search results page using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the quick filter chips to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 3: Tap the "Nonstop" chip in the quick filter section
- tapOn:
    index: 0  # Assuming the first chip corresponds to "Nonstop"

# Step 4: Wait for the mini-filter tray to appear
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 5: Assert that the mini-filter tray is visible
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
# Step 6: Assert that the Apply/Done button in the mini-filter tray is visible
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P0: SSR V4 Mini-Filter Tray Apply ===
appId: com.traveloka.android.staging
name: android-ssrv4-mini-filter-tray-apply
---

# Step 1: Stop the app to ensure a clean state
- stopApp

# Step 2: Launch the app and navigate to the flight search results page using a deeplink
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"

# Step 3: Wait for the results page to load by checking the visibility of a flight card
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 4: Wait for the mini-filter tray chip to become visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 5: Tap the first chip in the mini-filter tray to open the mini-tray
- tapOn:
    index: 0

# Step 6: Wait for the mini-filter tray (bottom sheet) to become visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 7: Tap the Apply button in the mini-filter tray
# [sanitized: bare tapOn removed — no target specified]

# Step 8: Wait for the results to reload by checking the visibility of a flight card
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 9: Assert that the flight cards are visible, confirming the results have reloaded
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P1: SSR V4 Mini-Filter Tray Dismiss ===
appId: com.traveloka.android.staging
name: android-ssrv4-mini-filter-tray-dismiss
---

# Step 1: Stop the app and navigate to the flight search results page using the deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the mini-filter tray trigger to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 3: Tap the first filter chip to open the mini-filter tray
# [sanitized: bare tapOn removed — no target specified]

# Step 4: Wait for the mini-filter tray to become visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 5: Swipe down on the mini-filter tray to dismiss it
- swipe:
    direction: down
    element:

# Step 6: Assert that the mini-filter tray is dismissed and the results are still visible
- extendedWaitUntil:
    notVisible:
    timeout: 5000
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P0: SSR V4 Sort by Duration (Shortest) ===
appId: com.traveloka.android.staging
name: android-ssrv4-sort-duration
---

# Test Case: SSR V4 Sort by Duration (Shortest)
# Description: Open sort tray, tap index 6 (Shortest Duration), verify reload
# Priority: p0

# Step 1: Stop the app and navigate to the Flight Search Results screen using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Open the sort tray
# [sanitized: bare tapOn removed — no target specified]
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 3: Select "Shortest Duration" sort option (index 6)
- tapOn:
    index: 6

# Step 4: Wait for the results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 5: Verify that the results are visible
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
---

# === P1: SSR V4 Sort by Earliest Arrival ===
appId: com.traveloka.android.staging
name: android-ssrv4-sort-arrival-earliest
---

# SSR V4 Sort by Earliest Arrival
# Description: Open sort tray, tap index 5 (Earliest Arrival), verify reload
# Priority: p1

# Step 1: Stop the app to ensure a clean state
- stopApp

# Step 2: Launch the app and navigate to the flight search results page using a deeplink
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"

# Step 3: Wait until the flight search results are visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 4: Tap on the sort button to open the sort tray
# [sanitized: bare tapOn removed — no target specified]

# Step 5: Wait until the sort tray becomes visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 6: Tap on the radio button at index 5 to select "Earliest Arrival"
- tapOn:
    index: 5 # Index 5 corresponds to "Earliest Arrival" (SORT_ARRIVAL_TIME_EARLIEST)

# Step 7: Wait for the flight results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Success Criteria: The results should reload sorted by "Earliest Arrival" without any crash.
---

# === P1: SSR V4 Sort by Latest Departure ===
appId: com.traveloka.android.staging
name: android-ssrv4-sort-latest-departure
---

# Test Case: SSR V4 Sort by Latest Departure
# Description: Open sort tray, select "Latest Departure" (index 3), and verify results reload successfully.

# Step 1: Stop the app to ensure a clean state
- stopApp

# Step 2: Launch the app and navigate to the flight search results page using a deeplink
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"

# Step 3: Wait until the flight search results page is fully loaded
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 4: Tap on the sort button to open the sort tray
# [sanitized: bare tapOn removed — no target specified]

# Step 5: Wait until the sort tray is visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 6: Tap on the "Latest Departure" sort option (index 3 in the sort tray)
- tapOn:
    index: 3  # Latest Departure (SORT_DEPARTURE_TIME_LATEST)

# Step 7: Wait for the flight results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 8: Verify that the results page is still visible after sorting
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
# End of Test Case
---

# === P1: SSR V4 Sort: Direct Flight First ===
appId: com.traveloka.android.staging
name: android-ssrv4-sort-direct-first
---

# SSR V4 Sort: Direct Flight First
# This test case verifies that the "Direct flight first" sort option can be applied successfully
# on the flight search results page without any crashes.

# Step 1: Stop the app to ensure a clean state
- stopApp

# Step 2: Launch the app and navigate to the flight search results page using a deeplink
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"

# Step 3: Wait until the flight search results are visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 4: Tap on the sort button to open the sort tray
# [sanitized: bare tapOn removed — no target specified]

# Step 5: Wait until the sort tray is visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 6: Tap on the "Direct flight first" sort option (index 2 in the sort tray)
- tapOn:
    index: 2  # Index 2 corresponds to "Direct flight first" as per FlightSortTrayWidgetPresenter.kt

# Step 7: Wait for the flight search results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 8: Verify that the results page is displayed with the sorted results
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
# End of test case
---

# === P0: SSR V4 Nonstop Chip Quick Filter ===
appId: com.traveloka.android.staging
name: android-ssrv4-nonstop-chip
---

# Step 1: Stop the app and navigate to the flight search results page using a deeplink
- stopApp
- openLink:
    link: "traveloka://flight/fullsearch?ap=SIN.JKTA&dt=20260617&ps=1.0.0&sc=ECONOMY"
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 2: Wait for the quick filter section to be visible
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 10000

# Step 3: Scroll horizontally to find the Nonstop chip if needed
- scroll  # Scroll horizontally to ensure the Nonstop chip is visible

# Step 4: Tap the Nonstop chip
- tapOn:
    index: 0  # Assuming Nonstop chip is at index 0

# Step 5: Wait for the results to reload
- extendedWaitUntil:
    visible:
      id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided — look up from existing cases or android-v3 source]
    timeout: 30000

# Step 6: Assert that the results are visible
- assertVisible:
    id: "MISSING_ID_CHECK_EXISTING_CASES_OR_SOURCE" # [sanitizer: id not provided]
