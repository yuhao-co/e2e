# Auto-generated Android Maestro Suite
# Generated: 2026-06-12T08:19:48.358Z
# 14 cases covering flight search results
# NOTE: Individual YAML files (<caseId>.yaml) are the primary execution method
#   This file is for reference and multi-document compatibility only.

# === P0: Search Results Page Loads ===
appId: com.traveloka.android.staging
name: android-results-smoke
---
# Test case for verifying the flight search results page loads correctly
id: android-results-smoke
name: Search Results Page Loads
description: Navigate from home → search → results and verify flight cards appear
priority: p0

# Step 1: Launch the app
- launchApp

# Step 2: Tap on the "Flights" tile on the home screen
- tapOn:
    text: "Flights"  # Home screen product tile has NO android:id — text match is the ONLY contract

# Step 3: Assert that the search tab is visible
- assertVisible:
    id: "search_tab"

# Step 4: Tap on the search tab
- tapOn:
    id: "search_tab"

# Step 5: Assert that the search button is visible
- assertVisible:
    id: "btn_search"

# Step 6: Tap on the search button
- tapOn:
    id: "btn_search"  # btn_search is the blue Search CTA

# Step 7: Assert that the flight results page header is visible
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"

# Step 8: Wait until at least one flight card is visible
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 9: Assert that at least one flight card is visible
- assertVisible:
    id: "flight_result_v4_inventory_card"  # Verify that at least one card loaded
---

# === P0: Direct Flight Filter ===
appId: com.traveloka.android.staging
name: android-results-filter-direct
---
# Step 1: Navigate to search results (precondition)
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_navbar_toolbar_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 10000

# Step 3: Tap the first quick_filter_item chip in recycler_view_quick_filter to open filter dialog
- tapOn:
    id: "flight_result_v4_quick_filter_cell"  # Assuming this is the first chip in the quick filter

# Step 4: Wait for layout_filter_dialog to appear (id, timeout: 10000)
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"
    timeout: 10000

# Step 5: Tap button_direct (id: button_direct in flight_result_revamp_filter_transit_layer.xml)
- tapOn:
    id: "button_direct"

# Step 6: Tap tvResult to apply filter (id: tvResult — Apply button)
- tapOn:
    id: "tvResult"

# Step 7: Wait for flight_result_v4_navbar_toolbar_title to reload (extendedWaitUntil Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 8: Assert at least one card visible (id: flight_result_v4_inventory_card)
- assertVisible:
    id: "flight_result_v4_inventory_card"
---

# === P0: Sort by Cheapest ===
appId: com.traveloka.android.staging
name: android-results-sort-cheapest
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_navbar_toolbar_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 3: Tap flight_result_v4_sort_button (Compose id) to open sort tray
- tapOn:
    id: "flight_result_v4_sort_button"

# Step 4: Wait for sort tray to appear
- extendedWaitUntil:
    visible:
      id: "rbg_sort"
    timeout: 30000

# Step 5: Tap the radio_button at index 0 (Cheapest) to sort by price ascending
- tapOn:
    id: "radio_button"
    index: 0

# Step 6: Wait for flight_result_v4_navbar_toolbar_title to reload
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 7: Assert flight_result_v4_inventory_card still visible
- assertVisible:
    id: "flight_result_v4_inventory_card"
---

# === P0: Select a Flight (View Detail) ===
appId: com.traveloka.android.staging
name: android-results-select-flight
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_inventory_card (Compose id, timeout: 30000)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 3: Tap first flight_result_v4_inventory_card (Compose id)
- tapOn:
    id: "flight_result_v4_inventory_card"  # Tapping the first flight card to view details

# Step 4: Wait for detail screen to appear (new screen or bottom sheet)
- extendedWaitUntil:
    visible:
      id: "flight_summary_activity_ticket_option_section"  # Assuming this ID appears on the detail screen

# Step 5: Assert we are no longer on the plain results list (takeScreenshot for verification)
- assertVisible:
    id: "flight_summary_activity_ticket_option_section"  # Verifying we are on the detail screen
- takeScreenshot: "flight_detail_view"  # Taking a screenshot for verification
---

# === P1: 1-Stop Filter ===
appId: com.traveloka.android.staging
name: android-results-filter-one-stop
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Open filter dialog and apply 1-stop filter
- tapOn:
    id: "quick_filter_item"  # Tap quick_filter_item to open filter dialog
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"  # Wait for filter dialog to be visible
    timeout: 30000
- tapOn:
    id: "button_one_transit"  # Tap button_one_transit to apply 1-stop filter
- tapOn:
    id: "tvResult"  # Tap tvResult to apply the filter
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"  # Wait for results to reload
    timeout: 30000

# Step 3: Assert that flight cards are still visible after applying the filter
- assertVisible:
    id: "flight_result_v4_inventory_card"  # Assert that flight cards are visible after applying the filter
---

# === P1: Sort by Fastest ===
appId: com.traveloka.android.staging
name: android-results-sort-fastest
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_navbar_toolbar_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 3: Tap flight_result_v4_sort_button (Compose id) to open sort tray
- tapOn:
    id: "flight_result_v4_sort_button"

# Step 4: Wait for sort tray to appear
- extendedWaitUntil:
    visible:
      id: "rbg_sort"
    timeout: 30000

# Step 5: Tap the radio_button at index 1 (Fastest) to sort by duration ascending
- tapOn:
    id: "radio_button"
    index: 1

# Step 6: Wait for flight_result_v4_navbar_toolbar_title to reload
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 7: Assert cards still visible
- assertVisible:
    id: "flight_result_v4_inventory_card"
---

# === P1: Airline Filter via Bottom Sheet ===
appId: com.traveloka.android.staging
name: android-results-airline-filter
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Open filter dialog
- tapOn:
    id: "flight_result_v4_quick_filter_cell"  # Tap quick_filter_item to open filter dialog
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"  # Wait for filter dialog to appear

# Step 3: Scroll to airline section
- scroll  # Scroll to layer_airline section

# Step 4: Select first airline checkbox
- tapOn:
    id: "layer_airline"  # Tap first airline checkbox inside recycler_view_content under layer_airline

# Step 5: Apply filter
- tapOn:
    id: "tvResult"  # Tap tvResult to apply

# Step 6: Wait for results to reload
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"  # Wait for flight_result_v4_navbar_toolbar_title to reload

# Success Criteria: Verify that the airline filter has been applied and cards are updated
- assertVisible:
    id: "flight_result_v4_inventory_card"  # Assert that flight cards are visible after applying the filter
---

# === P1: Departure Morning Filter ===
appId: com.traveloka.android.staging
name: android-results-departure-time-filter
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_navbar_toolbar_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 3: Tap quick_filter_item to open filter dialog
- tapOn:
    id: "flight_result_v4_filter_button"  # Filter button to open filter dialog

# Step 4: Wait for layout_filter_dialog (id)
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"
    timeout: 30000

# Step 5: Scroll to layer_time section (id: layer_time)
- scroll

# Step 6: Tap button_departure_morning (id: button_departure_morning)
- tapOn:
    id: "button_departure_morning"

# Step 7: Tap tvResult to apply (id: tvResult)
- tapOn:
    id: "tvResult"

# Step 8: Wait for flight_result_v4_navbar_toolbar_title reload
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Success Criteria: Verify that the morning departure filter is applied
- assertVisible:
    id: "flight_result_v4_inventory_card"  # Ensure that the results are updated with the applied filter
---

# === P1: Reset All Filters ===
appId: com.traveloka.android.staging
name: android-results-reset-filters
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"  # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"  # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_navbar_toolbar_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 3: Tap quick_filter_item to open filter dialog
- tapOn:
    id: "flight_result_v4_filter_button"  # Tap the filter button to open the filter dialog

# Step 4: Wait for layout_filter_dialog (id)
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"
    timeout: 30000

# Step 5: Tap button_direct (id) to select direct filter
- tapOn:
    id: "button_direct"  # Select the direct filter

# Step 6: Tap tvReset to reset all filters (id: tvReset)
- tapOn:
    id: "tvReset"  # Reset all filters

# Step 7: Tap tvResult to apply with no filters (id: tvResult)
- tapOn:
    id: "tvResult"  # Apply the filters (which are now none)

# Step 8: Wait for flight_result_v4_navbar_toolbar_title reload
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 9: Assert flight_result_v4_inventory_card visible
- assertVisible:
    id: "flight_result_v4_inventory_card"  # Verify that the flight cards are visible after resetting filters
---

# === P1: Scroll Through Results ===
appId: com.traveloka.android.staging
name: android-results-scroll
---
# Step 1: Launch the app and navigate to the flight search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Scroll through the results
- scroll              # Swipe up to load more flight cards
- scroll              # Swipe up again to load more flight cards

# Step 3: Assert that flight cards are still visible after scrolling
- assertVisible:
    id: "flight_result_v4_inventory_card"  # Ensure that flight cards are still visible after scrolling
---

# === P1: View Price Calendar ===
appId: com.traveloka.android.staging
name: android-results-price-calendar
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_navbar_toolbar_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 3: Tap widget_dateflow (id: widget_dateflow in flight_result_revamp_activity.xml)
- tapOn:
    id: "flight_result_v4_date_flow_row"  # Correct ID for the date widget

# Step 4: Wait for price calendar view to appear (new screen or overlay)
- extendedWaitUntil:
    visible:
      id: "flight_summary_activity_ticket_option_section"  # Assuming this ID is part of the price calendar view
    timeout: 30000

# Step 5: Take screenshot for visual verification
- scroll  # Assuming scrolling is needed to capture the screenshot
- assertVisible:
    id: "flight_summary_activity_ticket_option_section"  # Verifying the price calendar view is visible
---

# === P2: 2+ Stops Filter ===
appId: com.traveloka.android.staging
name: android-results-filter-multi-stop
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_navbar_toolbar_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 3: Tap quick_filter_item to open filter dialog
- tapOn:
    id: "flight_result_v4_filter_button"  # Tap the filter button to open the filter dialog

# Step 4: Wait for layout_filter_dialog (id)
- extendedWaitUntil:
    visible:
      id: "layout_filter_dialog"
    timeout: 30000

# Step 5: Tap button_two_transit (id)
- tapOn:
    id: "button_two_transit"  # Apply 2+ Stops filter

# Step 6: Tap tvResult to apply (id)
- tapOn:
    id: "tvResult"  # Apply the filter selections

# Step 7: Wait for flight_result_v4_navbar_toolbar_title reload
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Success Criteria: Verify that the 2+ stop filter is applied
- assertVisible:
    id: "flight_result_v4_inventory_card"  # Check that the results are visible after applying the filter
---

# === P2: Sort by Best ===
appId: com.traveloka.android.staging
name: android-results-sort-best
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_navbar_toolbar_title (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Step 3: Tap flight_result_v4_sort_button (Compose id) to open sort tray
- tapOn:
    id: "flight_result_v4_sort_button"

# Step 4: Wait for sort tray to appear
- extendedWaitUntil:
    visible:
      id: "rbg_sort"
    timeout: 30000

# Step 5: Tap the radio_button at index 2 (Best) to sort by best value
- tapOn:
    id: "radio_button"
    index: 2

# Step 6: Wait for flight_result_v4_navbar_toolbar_title to reload
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"
    timeout: 30000

# Success Criteria: Best sort applied, no crash
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
---

# === P2: View Flight Detail (Card Tap) ===
appId: com.traveloka.android.staging
name: android-results-view-detail
---
# Step 1: Navigate to search results
- launchApp
- tapOn:
    text: "Flights"      # Home screen product tile has NO android:id — text match is the ONLY contract
- assertVisible:
    id: "search_tab"
- tapOn:
    id: "search_tab"
- assertVisible:
    id: "btn_search"
- tapOn:
    id: "btn_search"     # btn_search is the blue Search CTA (hangar_widget.xml line 209)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 2: Wait for flight_result_v4_inventory_card (Compose id)
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_inventory_card"
    timeout: 30000

# Step 3: Tap flight_result_v4_inventory_card (Compose id)
- tapOn:
    id: "flight_result_v4_inventory_card"

# Step 4: Wait for detail overlay or new screen
- extendedWaitUntil:
    visible:
      id: "flight_result_v4_navbar_toolbar_title"  # Assuming this ID is present in the detail view

# Step 5: Take screenshot
- screenshot:
    path: "test-results/android/android-results-view-detail.png"

# Step 6: Tap back to return to results (system back)
- back

# Step 7: Assert flight_result_v4_navbar_toolbar_title visible again (Compose id)
- assertVisible:
    id: "flight_result_v4_navbar_toolbar_title"
